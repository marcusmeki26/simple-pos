import Components.LoginFrame;
import Database.DBConnection;

import java.sql.Connection;

public class Main {
    public static void main(String[] args) {
        new LoginFrame(); // creates a frame
    }
}